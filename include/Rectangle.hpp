#pragma once

#include <string>

class Rectangle {
    private:
    public:
        Rectangle(const std::string& input);
        int x;
        int y;
        int width;
        int height;
        int id;
};
